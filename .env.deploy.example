# Frontend deployment credentials (gitignored — copy to .env.deploy and fill in)
# Used by: npm run deploy

# FTPS host (cPanel)
FTP_HOST=192.199.240.234
FTP_PORT=21
FTP_USER=jewelcar
FTP_PASS=

# Where to upload (no trailing slash)
REMOTE_DIR=/public_html/staging.jewelcart.shop

# Local build directory
LOCAL_DIST=dist
